/* Main default import (state), should be named for importing convenience */

export default interface IProcessingState {
  readonly [key: string]: boolean;
}

/* Other named exports */

// export {};
