import {
  selectRawExpenses,
  selectExpenses,
  selectExpensesPagination,
  selectExpensesTotal,
} from "app/expenses/expenses.selector";

describe("expenses.selector", () => {
  test("selectRawExpenses should...", () => {});

  test("selectExpenses should...", () => {});

  test("selectExpensesPagination should...", () => {});

  test("selectExpensesTotal should...", () => {});
});
