import { selectRawErrors, selectErrorText } from "app/error/error.selector";

describe("error.selector", () => {
  test("selectRawErrors should...", () => {});

  test("selectErrorText should...", () => {});
});
